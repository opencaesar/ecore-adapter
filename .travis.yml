language: java
install: true

jdk:
- openjdk8

before_script:
- cd ecore-adapter

script:
  - ./gradlew build

deploy:
  - provider: script
    script: ./gradlew bintrayUpload
    skip_cleanup: true
    dry-run: false
    on:
      tags: true
      repo: opencaesar/ecore-adapter
      branch: master