language: java
install: true

jdk:
- oraclejdk8

before_script:
- cd io.opencaesar.ecore.adapter

script:
  - ./gradlew build

deploy:
  - provider: script
    script: ./gradlew bintrayUpload
    skip_cleanup: true
    dry-run: false
    on:
      tags: true
      repo: opencaesar/ecore-adapter
      branch: master